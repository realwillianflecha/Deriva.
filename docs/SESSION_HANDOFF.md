# Deriva — Contexto de sesión (para retomar sin perder nada)

> Este archivo es un resumen de handoff, pensado para que una sesión nueva (o esta misma, después de comprimir el historial) pueda retomar el trabajo sin perder contexto importante que no vive en el código. El detalle técnico profundo (arquitectura, fórmulas, bugs encontrados) está en `docs/GAME_DESIGN.md` — este archivo es el complemento "quién es el usuario, cómo llegamos hasta acá, qué falta".

## Qué es el proyecto, en una frase

**Deriva**: simulador espacial educativo en 3D (Next.js + React Three Fiber) donde el jugador pilotea libremente una nave (mouse-look + WASD, primera/tercera persona) entre los **planetas rocosos del sistema solar** (Mercurio, Venus, Tierra, Marte — excluyendo los gaseosos), con decisiones narrativas y datos reales de la NASA/SpaceX en el camino.

**Importante — alcance real, no asumir que es "solo Marte":** los 4 planetas rocosos (Tierra/Venus/Mercurio/Marte) están en la escena, a escala y distancia real entre sí, y los 4 tienen aterrizaje físico real funcionando. Solo Marte tiene misión narrativa completa (briefing/llamarada/elegir sitio/debrief) — Tierra/Venus/Mercurio aterrizan directo sin diálogo. No lo presentes como "el juego de Marte".

## Quién es el usuario / cómo prefiere trabajar

- Habla en español rioplatense informal ("vos", "dale", "que onda"). Respondé en el mismo registro, sin acartonarse.
- Cero experiencia previa en desarrollo de videojuegos, pero sí en desarrollo web — el pedido explícito desde el arranque fue **explicar cada concepto nuevo con su equivalente de desarrollo web** (ej. WebGL ≈ DOM crudo, three.js ≈ React para WebGL, floating origin ≈ reseteo de scroll infinito). Mantené esto — es el objetivo pedagógico del proyecto, no un detalle de una sola vez.
- Le gusta iterar rápido y a veces delega decisiones de diseño explícitamente ("vos decidís", "tirame lo que pegue") — en esos casos, comprometeté con una recomendación concreta y justificada, no un menú de opciones.
- Cuando pregunta "por qué"/"explicame", quiere números concretos y razonamiento real, no una respuesta superficial — el estilo que funcionó fue: explicación con analogía web + números concretos (ej. la tabla de precisión de float32, el cálculo de ULP a distintas magnitudes).
- Valora que se verifique todo jugando/probando de verdad, no solo con build/lint limpios — varias veces encontramos bugs reales (no solo de balance) usando tests automatizados con Puppeteer simulando input real. Seguí haciendo esto antes de dar algo por confirmado.
- Pidió explícitamente no inicializar git hasta que él lo pidiera (lo pidió más adelante, ya está resuelto — ver abajo). Este tipo de instrucciones de secuenciación ("hacelo cuando yo diga") hay que respetarlas al pie de la letra.
- **Flujo de git, instrucción durable**: sin ramas (es el único programador, no hacen falta) — siempre directo a `main`. Pero el commit+push en sí **solo cuando lo autoriza explícitamente, cada vez** — no asumir que "podés seguir commiteando solo" nunca, ni siquiera después de terminar una feature grande. Terminar el trabajo, verificar, y preguntar/esperar antes de tocar git. (Guardado también en memoria persistente: `feedback_git_workflow.md`.)
- Cuando pide guardar contexto antes de comprimir el chat (como ahora), el lugar es este archivo — actualizarlo, no crear uno nuevo.

## Repo / deploy

- GitHub: `https://github.com/realwillianflecha/Deriva..git` — **ojo, el nombre del repo tiene un punto al final** ("Deriva.", no "Deriva"), no es un typo a corregir, así lo creó el usuario y así está configurado el remoto local.
- Branch `main`. **Conectado a Vercel** — cada push a `main` dispara un deploy automático, no hace falta ningún paso manual extra ni configuración en el dashboard (sin variables de entorno, todo es cliente).
- No hay CI/CD ni tests automatizados — la verificación es manual + scripts puntuales de Puppeteer que se escriben y tiran en `scratchpad`, no viven en el repo. Patrón fijo: `page.mouse.click(x,y)` real (nunca `.click()` sintético, porque Pointer Lock lo rechaza), y para leer estado del juego durante un test se agregan hooks temporales tipo `window.__gameStore`/`window.__ship` en el componente relevante, **y se sacan antes de terminar** (nunca quedan commiteados).
- Último commit al momento de escribir esto: `e716ef0` — "Add atmospheric entry/landing sequence with real per-planet gravity". Árbol de trabajo limpio, pusheado.

## Timeline de esta sesión (orden real de los hechos)

1. **v1 — vertical slice inicial**: Next.js + R3F desde cero. Lanzamiento vertical tipo cohete (empuje vs. gravedad, Max-Q) + tránsito automático + aterrizaje tipo Lunar Lander + narrativa (briefing → llamarada solar → elegir sitio → debrief). Bug real encontrado y arreglado: `LANDING_THRUST` copiado del lanzamiento, 4x más fuerte que la gravedad marciana real → la nave se disparaba sin control.
2. **Rediseño a vuelo libre (v2)**: el usuario dijo que v1 "no se parece en nada a lo que quiero" — pidió cámara libre, movimiento en cualquier dirección, espacio más amplio, tamaños/texturas reales, cámara 1ra/3ra persona alternable. Se reemplazó toda la física vertical por un único `FlightController` (mouse-look + WASD, Pointer Lock API, sin gravedad planetaria, sin estados de choque). Se agregó un modelo real de nave (Falcon Heavy, CC-BY, de Poly Pizza) y texturas reales (Solar System Scope, CC BY) para Tierra/Marte/Sol/skybox.
3. **Pulido de sensación de vuelo**: el usuario reportó 3 problemas jugando de verdad — combustible insuficiente incluso con tanque extra, cámara que "se alejaba" al acelerar, giro demasiado rápido/poco realista. Causas reales encontradas: (a) bug de `clamp` que recortaba el bono de combustible extra a 100 en el primer frame, (b) `lerp` de posición de cámara que generaba arrastre visible, (c) rotación de la nave aplicada instantáneamente en vez de suavizada. Los tres se corrigieron (ver `GAME_DESIGN.md`).
4. **Nombre del proyecto**: pasó de "Misión Marte" (nombre de trabajo) a **"Deriva"** — decisión final del asistente a pedido explícito del usuario ("decidí vos, algo cool y original").
5. **Git + GitHub**: se inicializó el repo, se conectó al remoto que el usuario ya había creado en GitHub, primer push exitoso.
6. **Corrección de alcance**: al pedir nombre/descripción para el repo, el usuario aclaró que el juego es sobre **todos los planetas rocosos**, no solo Marte — corregido el nombre/descripción, guardado en memoria.
7. **Escala de planetas — 3 iteraciones**: el usuario notó que los planetas se veían chicos. Se explicó el trade-off real (precisión de punto flotante en WebGL, por qué escala 100% real es impracticable, qué es floating origin) y se subió la escala en pasos seguros y verificados: radio de la Tierra 150 → 6.000 → **20.000 unidades** (relación nave:planeta final ~2.850:1). Cada salto implicó recalcular en cadena: distancia Tierra-Marte, radio de "llegada" (`ARRIVAL_RADIUS`), plano lejano de cámara, tamaño del skybox, y velocidad de la nave (para que el viaje no se sienta eterno). Se verificó en cada paso que no hubiera parpadeo por precisión (float32) volando a velocidad máxima con boost.
8. **Combustible, segunda vuelta**: al agrandar la distancia, el combustible se agotaba en el viaje de ida. El usuario sugirió correctamente bajar la tasa de consumo en vez de inflar el tanque (cambio cosmético) — ya se había arrancado por ahí; se terminó de ajustar y se verificó con un test de viaje completo apuntando de verdad a Marte (lección de testing: sostener W sin apuntar con el mouse da métricas sin sentido, ya pasó dos veces).
9. **Pregunta sobre el Sol**: confirmado que es una esfera 3D real (`sphereGeometry` + `meshBasicMaterial`, no un sprite/PNG plano) — el aspecto "chato" viene de que `meshBasicMaterial` no tiene sombreado. Quedó sin resolver por un tiempo (el usuario pasó a otra cosa antes de responder) y sigue sin resolverse — ver "Qué falta".
10. **Deploy a Vercel**: se conectó el repo, deploy automático en cada push confirmado funcionando, sin config extra necesaria.
11. **Mercurio y Venus agregados**: mismo patrón que Marte (`RADIUS_SCALE`/`REAL_RADIUS_KM`), texturas reales de Solar System Scope descargadas y verificadas. Sin arco de misión propio todavía.
12. **Bug real: Tierra y Venus con las esferas superpuestas.** Las posiciones de los 4 planetas se rediseñaron por completo: ahora salen de una única fórmula (`DISTANCE_SCALE` × distancia real al Sol de cada uno, los 4 alineados sobre una misma recta), en vez de vectores sueltos "a ojo" por planeta — garantiza matemáticamente que ningún par se superponga. Tierra-Marte quedó en 280.000 unidades (antes ~141.000); se recalibró velocidad de la nave (+40%) y consumo de combustible (-35%) para el viaje más largo.
13. **Feature grande: secuencia de entrada atmosférica y aterrizaje.** Pedido explícito: al acercarse a un planeta, un destello blanco dramático (con sonido, primer audio del proyecto) tapa el cambio a un mundo de superficie nuevo, donde la nave cae con la gravedad REAL de ese planeta y solo se puede frenar/impulsar en vertical (sin WASD, cámara completamente libre desacoplada de la orientación de la nave). Se armó un plan formal (`/plan`, validado con un segundo pase de revisión) antes de tocar código — encontró 3 bugs reales de diseño antes de implementar (Tierra disparando el aterrizaje al instante, un frame de vacío antes del destello, pointer lock rompiéndose en silencio). Detalle completo en `GAME_DESIGN.md`, sección "Secuencia de aterrizaje".
14. **Iteración sobre la feature de aterrizaje, todo por reportes reales jugando**: (a) la nave quedaba acostada al pararse (identidad ≠ parado, hacía falta rotar 90°) y después medio hundida en el piso (el offset de recentrado de vuelo no sirve para "base tocando el piso" — se corrigió midiendo la caja envolvente real del modelo con `Box3`, no un número a mano); (b) el juego pasó a arrancar parado en la Tierra en vez de flotando en el espacio (cambio de rumbo pedido explícitamente); (c) se deshabilitó `MissionLog` (el panel de fin de misión) sin borrarlo, "molesta"; (d) la nave se clavaba en seco durante el destello (ningún controlador corría durante `entering`/`exiting` — se corrigió haciendo que sigan de largo con la última velocidad conocida); (e) la caída ahora arranca a velocidad máxima en vez de acelerar desde cero, como pidió el usuario ("no tiene sentido que empiece desde cero, como si hubiera empezado a acelerar").

## Estado actual del código (resumen — el detalle vive en GAME_DESIGN.md)

- Fases: `briefing` → `flight` → `debrief`. Eje de estado nuevo e independiente, `flightMode` (`gameStore.ts`): `space → entering → descending → landed → ascending → exiting → space` — gobierna qué mundo/controlador está activo, sin tocar `phase`.
- El juego **arranca `flightMode:'landed'` en la Tierra**, no en el espacio — despegar (`start-flight`) dispara un ascenso real.
- Escala: `RADIUS_SCALE` (radios) + `DISTANCE_SCALE` (distancias entre planetas, alineados sobre una recta desde su distancia real al Sol) en `src/content/planets/planetData.ts` — ver "Escala de los planetas" en `GAME_DESIGN.md`.
- Vuelo libre: Pointer Lock + mouse-look con suavizado (slerp), WASD + Space/Ctrl + Shift boost, cámara 1ra/3ra persona con tecla `C`, zoom con rueda del mouse en 3ra persona. Sin cambios de fondo.
- **Aterrizaje** (`DescentController.tsx`, nuevo, hermano de `FlightController.tsx`): sin WASD, solo Espacio contra la gravedad real del planeta (`PLANETS[id].gravity`, recién usado por primera vez). Cámara desacoplada de la nave vía `cameraLookRef` (un `Quaternion` compartido). Mundo de superficie (`SurfaceWorld.tsx`) deliberadamente básico (un plano de color sólido) — próxima iteración, no esta.
- **Destello de transición** (`AtmosphericTransition.tsx`): tapa el swap de mundo, nunca un spinner. Primer sonido del proyecto (`public/audio/atmospheric-entry.mp3`, Mixkit, licencia libre).
- Narrativa sin cambios de contenido: briefing (elegir combustible) → llamarada solar a mitad de camino → elegir sitio en Marte (Jezero/Olympus) → aterrizaje físico real → debrief (panel de datos deshabilitado, no se muestra).

## Qué falta / próximos pasos posibles

- **Sin resolver, viejo**: ¿sumarle relieve/glow visual al Sol? (pregunta de hace varias sesiones, nunca respondida)
- **Bajarse de la nave y caminar como persona**: ahora sí hay una secuencia de aterrizaje física real sobre la que engancharlo (antes no existía nada). Es el siguiente salto grande de alcance — necesita character controller y terreno real (hoy `SurfaceWorld` es un plano de un solo color a propósito).
- **Mission arc propio para Mercurio/Venus**: hoy aterrizan directo sin diálogo, solo Marte tiene narrativa completa.
- **`MissionLog` deshabilitado**: sin forma de volver al briefing tras terminar en Marte salvo recargar la página — aceptado a propósito, no se agregó reemplazo.
- Pulido de sensación de vuelo pendiente de la lista original: conectar `leva` (instalado, sin usar); mejorar la cabina en primera persona (3 cajas simples). El usuario preguntó en algún momento sobre mejorar el modelo de la nave en Blender — se le recomendó NO hacerlo (curva de aprendizaje no vale la pena ahora), mejor sourcing de un modelo mejor con licencia libre, mismo criterio que se usó para el Falcon Heavy actual.
- Sin música de fondo (sí hay un efecto puntual), sin soporte mobile/táctil, sin accesibilidad, sin persistencia/guardado — fuera de alcance a propósito.
